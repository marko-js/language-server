import B from "<A>";
import Bc from "<Ab>";

<A/>
<Ab/>
<B/>
<Bc/>

<B>bound to the import, so an identifier tag</B>
<I>not bound, so an uppercase native tag</I>
<Missing>not bound and no tag file, so a missing component error</Missing>
